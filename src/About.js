import React, { Component } from 'react'

class About extends Component {
  constructor(props){
    super(props)
  }
  render() {
    return (
      <div>
        <h2>This is an app to track stocks, made in React. An API will be attached one day, maybe!</h2>
      </div>
    );
  }
}

export default About;
